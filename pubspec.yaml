name: boo_boo_app
description: Aplikasi Resolusi Konflik Pasangan
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # Tidak ada assets atau fonts untuk minimal setup